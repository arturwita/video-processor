import { Injectable } from "@nestjs/common";
// import * as ffmpeg from "fluent-ffmpeg";

@Injectable()
export class FFmpegService {
  public async todo(): Promise<any> {
    // return new Promise((resolve, reject) => ffmpeg(videoUrl, {}));
  }
}
